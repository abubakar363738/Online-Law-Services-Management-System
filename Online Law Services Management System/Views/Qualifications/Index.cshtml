@model IEnumerable<Online_Law_Services_Management_System.Models.Qualification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DegreeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.University)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CompletionDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DegreeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.University)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CompletionDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.QualificationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.QualificationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.QualificationId })
        </td>
    </tr>
}

</table>
